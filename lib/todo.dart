import 'package:flutter/material.dart';

class todo {
  String id;
  String title;
  String subject;
  DateTime date;
  todo(
      {required this.id,
      required this.title,
      required this.subject,
      required this.date});
}
